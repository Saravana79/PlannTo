$("#page_pagination").val("<%= (params[:page].to_i + 1).to_s %>");
element= jQuery('#content_next')
if (<%=@items.results.size%> < 10)
{
	running = true;
}
else{

running=false;
}

<% unless params[:type] %>
 $("#content").html('<%= escape_javascript(render(:partial => 'search/index')) %>');
<%else%> 
 $("#content").append('<%= escape_javascript(render(:partial => 'search/index')) %>');
<%end%> 

