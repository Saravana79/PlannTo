$("#page_pagination").val("<%= (params[:page].to_i + 1).to_s %>");

if (<%=@items.results.size%> < 10)
{
	running = true;
}
else{

running=false;
}
 $("#search_items").append('<%= escape_javascript(render(:partial => 'search/search_items')) %>');


