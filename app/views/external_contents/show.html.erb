<!DOCTYPE html>
<html>
	<head>
		<meta charset="utf-8">
		<meta http-equiv="X-UA-Compatible" content="IE=edge,chrome=1">
		<title><%= content_for?(:title) ? yield(:title) : "PlannTo - #{@content.title}" %></title>
 
		<meta name="description" content="">
		<%= csrf_meta_tags%>

		<%= stylesheet_link_tag "external_contents" %>
		<script src="http://html5shim.googlecode.com/svn/trunk/html5.js"></script>		
		
		<%= csrf_meta_tags %>
</head>

<body>
    <div id="topbar">
      <div class='container clearfix'>
         <div class='float_lft'> <img src="/images/logo.png"> </div>    
         <div class='float_lft padding-left'> 
           <%=render :partial => "votes/voting_detail", :locals => {:item => @content, :user => user_signed_in? ? current_user : nil}%>
         </div>
         <div class='float_lft padding-left padding-top'> 
           <%= link_to "Write comments", content_path(@content) %>
         </div>
         <div class='float_rgt padding-top'>
           <%= link_to image_tag('icon_close_hover.png'), content_path(@content) %>
         </div>
      </div>  
    </div> 
      
    <div id='content_body'>
      <iframe id="iframe" src="<%=@content.url %>"</iframe>
    </div>  
   
</body>

 
</html>
