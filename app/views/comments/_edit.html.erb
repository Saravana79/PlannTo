<div class="float_rgt"><img src="<% if user_signed_in? %> <%= current_user.get_photo %> <%end%>" class="userImageSmall">
	</div>
	<%= form_for  @comment, :url => content_comment_path(content,@comment), :remote => true, :html => { :id => "comment_form_#{content.id}"},:method => :put do |f| -%>
	<%= f.text_field :comment, :class => "txt_gray new_comment", :size => "80", :id => "comment_id#{content.id}" %>
	<% end -%> 
