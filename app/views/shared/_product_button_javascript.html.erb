<script type="text/javascript">
<%= render "shared/javascript_button", :formate => :js, :button_class => button_class, :item => item, :related_follower => related_follower %>
<% if current_user.blank? %>
    Product.load_signout_user_link();
<% end %>
var item_id = <%= item.id %>;
var url = "<%= feed_contents_path(:items => item.id  ) %>";
  $("#usual2 ul").idTabs("tabs1");
  Product.detailed_specification();

$(".PlanntoBuyBtn span[title]").tooltip();

  $(document).ajaxError(function(e, XHR, options){
    if (XHR.status == 401){
      $("#dialog-form").dialog("open");
    }
  });
</script>