<script type="text/javascript">
  <% if current_user.blank? %>
   // Product.login_dialog();
    //$(".ui-dialog-titlebar").hide();
  <% if !item.blank? %>
 // $(".ui-dialog-titlebar").hide();
    $("#plan_to_buy_<%= item.id %>").click(function(){
    $('#dialog-form').bPopup({
    closeClass:'Closebut',
    position: [screen.width / 3, $(window).scrollTop() + 150],
    follow: [false, false] //x, y
 });
 return false;
 });
 
 $("#plan_to_own_<%= item.id %>").click(function(){
   $('#dialog-form').bPopup({
   closeClass:'Closebut',
   position: [screen.width / 3, $(window).scrollTop() + 150],
   follow: [false, false] //x, y
 });
 return false;
 });
 
 $("#plan_to_follow_<%= item.id %>").click(function(){
   $('#dialog-form').bPopup({
   closeClass:'Closebut',
   position:[screen.width / 3, $(window).scrollTop() + 150],
   follow: [false, false] //x, y
 });
 return false;
 });
 
  $("#plan_to_buy_select_<%= item.id %>").click(function(){
   $('#dialog-form').bPopup({
   closeClass:'Closebut',
   position: [screen.width / 3, $(window).scrollTop() + 150],
   follow: [false, false] //x, y
 });
 return false;
 });
   //Product.show_login_dialog("#plan_to_buy_<%= item.id %>, #plan_to_own_<%= item.id %>, #plan_to_follow_<%= item.id %>, #plan_to_buy_select_<%= item.id %>");
  <% end %>
 // Product.show_login_dialog(".sign_in")
  //$(".ui-dialog-titlebar").hide();
  <% else %>
   // Product.hide_login_dialog();
    //$(".ui-dialog-titlebar").hide();
  <% end %>
</script>
