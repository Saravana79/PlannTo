 $("#proposal").html("<%= escape_javascript(render 'proposal') %>");
 $(".Closebut").show();
 $("#proposal").bPopup({
    closeClass:'Closebut',
    modalClose: false,
    position: [(screen.width / 2) - (520/2),$(window).scrollTop() + 70],
    follow: [false, false] //x, y
 });
