<%if current_user.is_admin? %>
  $("#contentEditView").html('');
  $("#contentEditView").html("<%= escape_javascript(render '/shared/content_reject_reason',:detail => @detail) %>");
  $("#contentEditView").bPopup({
    closeClass:'Closebut',
    modalClose: false,
    position: [(screen.width / 2) - (520/2), $(window).scrollTop() + 120],
    follow: [false, false] //x, y
 });
<%else%>
 <%if @detail == "true" %>
   window.location.href = "<%= "/#{@itemtype.downcase.pluralize}"
 %>"
 <%else%>  
   $("#mainContentDiv<%=@content.id%>").remove();
 <%end%>
<%end%>
  
