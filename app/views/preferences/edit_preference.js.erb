$("#giveAdviceDialog").html('<%= escape_javascript(render(:partial => '/preferences/edit_preference')) %>');
$(".Closebut").show();
$("#giveAdviceDialog").bPopup({
    closeClass:'Closebut',
    modalClose: false,
    position: [screen.width / 18, $(window).scrollTop() + 120],
    follow: [false, false] //x, y
 });
