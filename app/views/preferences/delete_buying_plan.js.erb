
<%if params[:type] == "complete" %>
$("#owned_item").html("<%= escape_javascript(render 'preferences/owned_item_select', {:item => @item}) %>");
$("#owned_item").dialog({
height: 620,
width:600});
$(".Close_dialog").show();
$(".ui-dialog-titlebar").hide();


<%else%> 
window.location.href = "/<%=current_user.username%>?follow=Preferences";

<%end%>
