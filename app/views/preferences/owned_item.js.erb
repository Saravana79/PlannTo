  //window.location.reload( true );
  $("#owned_item").html("<%= escape_javascript(render 'preferences/owned_item_description_save', {:item => @item}) %>");
$("#owned_item").bPopup({
    closeClass:'Closebut',
    modalClose: false,
    position: [(screen.width / 2) - (520/2), $(window).scrollTop() + 120],
    follow: [false, false] //x, y
 });
