<% if @valid %>
$("#plantobuyContent").html('<%= escape_javascript(render 'plan_to_buy_items', :items => get_items_with_constraint(@follow_item, @follow_types)) %>');
<%else%>
alert("You already added this Item");
<%end%>

