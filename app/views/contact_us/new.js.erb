$("#report_no_item").html("");
$("#report_no_item").html("<%= escape_javascript(render "contact_us/new") %> ");
$('#report_no_item').bPopup({
    closeClass:'Closebut',
    modalClose: false,
    position: [screen.width / 4, $(window).scrollTop() + 120],
    follow: [false, false] //x, y
 });
        
