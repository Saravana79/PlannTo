<!DOCTYPE html>
<head>
  <meta http-equiv="content-type" content="text/html; charset=UTF-8">
  <title>Advt <%= params[:size] %></title>
  <%= javascript_include_tag "http://ajax.googleapis.com/ajax/libs/jquery/1.7.1/jquery.min.js" %>
</head>
<body>
<div class="main_div">
  <div class="static_plannto_ad">
    <% if @ad.advertisement_type == "static" %>
      <%= link_to(image_tag("http://cdn1.plannto.com/static/images/amazon-01-mpu.jpg", :style => "height:240px;"), APP_URL + history_details_path(:ads_id => @ad.id, :iid => @impression_id, :ref_url => @ref_url), :id => "history_detail_link", :target=>"_blank")%>
    <% else %>
      <%= link_to(image_tag(@image.avatar.url, :style => "height:240px;"), APP_URL + history_details_path(:ads_id => @ad.id, :iid => @impression_id, :ref_url => @ref_url), :id => "history_detail_link", :target=>"_blank")%>
    <% end %>
  </div>
</div>

</body>
</html>



<style type="text/css">
    .main_div {
        cursor: pointer;
    }

    .main_div {
       /*
        width: <%= @iframe_width %>px !important;
        height: <%= @iframe_height %>px !important;*/
        text-align: center;
        overflow: hidden;

    }
</style>