!!!
%html{:lang => "en"}
  %head
    %meta{:charset => "utf-8"}/
    %title= yield(:title) + "PlannTo"
  %body    
    :javascript
      // to close the facebook popup on callback 
      if(window.opener) {
        window.opener.location.href = "#{profile_url(username: current_user.username)}"
        window.close()
      }    
