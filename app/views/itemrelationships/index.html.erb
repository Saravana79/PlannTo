<h1>Listing itemrelationships</h1>

<table>
  <tr>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @itemrelationships.each do |itemrelationship| %>
  <tr>
    <td><%= link_to 'Show', itemrelationship %></td>
    <td><%= link_to 'Edit', edit_itemrelationship_path(itemrelationship) %></td>
    <td><%= link_to 'Destroy', itemrelationship, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Itemrelationship', new_itemrelationship_path %>
