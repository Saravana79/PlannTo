
<!-- buttons - Start -->
<%= render 'shared/follow_buttons', :follow_type => 'Manufacturer', :follow_html => 'button' %>

</div>

<!-- buttons - End -->



<div class="leftcolumnbig">
<!-- Product Details - Start -->

  <div class="productdetails" style="float: left">
    <%= render @item.car_groups.last.class.superclass.name.underscore + '_image', :image_type => 'Manufacturer' unless @item.car_groups.blank? %>
    <span class="product_details">
      <h1><%= @item.name %></h1>
    </span>
  </div>

<br/>
<!-- Product Details - End -->

  <%= render :partial => "shared/review" %>

<!-- Tabs - Review Car | Ask Question | Share a Trip - End -->
<br/>
<!-- Overview Tabs -  Start -->

  <div id="usual2" class="usual">
    <ul class="tabs">
      <li class="tabstyle"><a href="#tabs1">All Models</a></li>
      <li class="tabstyle"><a class="" href="#tabs2" id="specification">Specification</a></li>
      <li class="tabstyle"><a href="#tabs3" class="">Questions</a></li>
    </ul>
  </div>

<div id="tabs1" style="display: block; " class="main-content-section">


  <%= render @item.car_groups.last.class.superclass.name.underscore unless @item.car_groups.blank? %>
</div>

  <div id="tabs2" style="display: none; " class="main-content-section">
  This specification tab
</div>
<div id="tabs3" style="display: none; ">Any Questions</div>
<!-- Q&A -  End -->

</div>





<div class="rightcolumnsmall">
<br/>
<div >


<a class="btns" href="#">Compare Cars</a>
<a class="btns" href="#">Ask Advices</a>
<a class="btns" href="#">Top Questions</a>

</div>
<br/>

<%=render :partial =>"items/related_products" %>
</div>










<div id="dialog-form" title="Create new user">
  <%= form_for(@user, :as => :user, :url => session_path("user"), :method => :post) do |f| %>
    <%= render "shared/login_fields", {:f => f} %>
  <% end %>
</div>


<script type="text/javascript">
  <% if @user_follow && @user_follow.follow_type == "Buyer" %>
  Product.load_buyer();
  <% end %>
  <% if @user_follow && @user_follow.follow_type == "Owner" %>
  Product.load_owner();
  <% end %>
  <% if @user_follow && @user_follow.follow_type == "Follow" %>
  Product.load_follow();
  <% end %>
  <% if current_user.blank? %>
  Product.load_signout_user_link();
  <% end %>

  Product.related_products('<%= related_products_product_path(@item, :follow_type => @user_follow ? @user_follow.follow_type : "") %>');

  $("#usual2 ul").idTabs("tabs1");
  Product.detailed_specification();
  <% if current_user.blank? %>
  Product.login_dialog();
  Product.show_login_dialog("#plan_to_buy, #plan_to_own, #plan_to_follow");
  <% else %>
  Product.hide_login_dialog();
  <% end %>
</script>